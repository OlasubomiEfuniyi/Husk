
#include <stdio.h>
#include <stdlib.h>

int main(void) {
  Tree tree = {NULL, NULL, NULL};

  Node node = calloc(1, sizeof(Node));

  print_tree(&tree);
  add_node(&tree, &node);
  print_tree(&tree);

  return 0;
}
